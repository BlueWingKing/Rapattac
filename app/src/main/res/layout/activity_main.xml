<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="crazie.com.example.jimmy.rapatac.MainActivity">

    <Button
        android:text="Login"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="23dp"
        android:id="@+id/loggin"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:onClick="Loggin" />

    <Button
        android:text="Kids"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/kids"
        android:layout_below="@+id/loggin"
        android:layout_alignLeft="@+id/loggin"
        android:layout_alignStart="@+id/loggin"
        android:layout_marginTop="24dp"
        android:onClick="kids" />

    <Button
        android:text="schema"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/schema"

        android:layout_marginTop="19dp"
        android:layout_below="@+id/kids"
        android:layout_alignLeft="@+id/kids"
        android:layout_alignStart="@+id/kids"
        android:onClick="Schema" />
</RelativeLayout>
